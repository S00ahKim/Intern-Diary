# 2020년 4월 3일

### paginate_queryset 유의점
- order_by를 해주지 않은 쿼리셋을 페이지네이션하면 매 페이지가 새로 불러와져서 결과가 매번 다르게 나온다.
- order_by의 기준이 되는 열은 변화가 생기면 안 된다. 변화가 생기면 호출 때마다 값이 바뀌어서 랜덤하게 호출되는 것과 별반 다를 바가 없다.

### 자바스크립트 현재시간 utc로 변환
```
new Date().toUTCString()
```

